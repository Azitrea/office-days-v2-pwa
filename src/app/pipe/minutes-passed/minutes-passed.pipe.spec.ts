import { MinutesPassedPipe } from './minutes-passed.pipe';

describe('MinutesPassedPipe', () => {
  it('create an instance', () => {
    const pipe = new MinutesPassedPipe();
    expect(pipe).toBeTruthy();
  });
});
