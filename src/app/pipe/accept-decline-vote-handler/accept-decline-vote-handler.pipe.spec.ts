import { AcceptDeclineVoteHandlerPipe } from './accept-decline-vote-handler.pipe';

describe('AcceptDeclineVoteHandlerPipe', () => {
  it('create an instance', () => {
    const pipe = new AcceptDeclineVoteHandlerPipe();
    expect(pipe).toBeTruthy();
  });
});
